# ms-neuron-pipeline dependencies
# Python >= 3.10 required
#
# Install third-party dependencies:
#   pip install -r requirements.txt
#
# Then install the local packages in editable mode:
#   pip install -e .

numpy
scipy
scikit-image
networkx

# Optional: used in diagnostic scripts/mcf.py for MCF-based skeletonization comparison
# mcf2swc
