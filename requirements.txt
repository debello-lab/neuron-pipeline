# ms-neuron-pipeline dependencies
# Python >= 3.10 required
#
# Install third-party dependencies:
#   pip install -r requirements.txt
#
# Then install the local packages in editable mode:
#   pip install -e .

numpy
scipy
scikit-image
networkx
crackle-codec
kimimaro
edt